---
minor_changes:
  - user - Expanded ``DOCUMENTATION`` for ``password_expires`` and ``expiry``
    with full descriptions, local-user-only restriction note, idempotency
    behavior, and epoch-conversion troubleshooting tip
    (`#217 <https://github.com/dell/ansible-powerscale/issues/217>`__).
  - user - Added ``EXAMPLES`` entries demonstrating security user with
    password expiration, service account without expiration, contractor
    account with account expiry, and check-mode + diff preview
    (`#217 <https://github.com/dell/ansible-powerscale/issues/217>`__).
  - user - Enriched ``RETURN`` descriptions for ``expired``,
    ``password_expired``, ``password_expiry``, and ``max_password_age``
    with policy context and usage guidance
    (`#217 <https://github.com/dell/ansible-powerscale/issues/217>`__).
  - user - Extended example playbook (``playbooks/modules/user.yml``) with
    end-to-end tasks for password expiration and account expiry, including
    idempotency checks, check-mode + diff preview, and cleanup
    (`#217 <https://github.com/dell/ansible-powerscale/issues/217>`__).
